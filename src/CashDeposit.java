// represents the deposit slot of the ATM
public class CashDeposit {

    // indicates whether the envelope with the cash was received (always return true as only a software simulation)
    public boolean isEnvelopeReceived() {
        return true;
    }
}
